# Build artifacts
node_modules/
dist/
build/

# Mirrored raw HTML (fetched owner-side; the parsed dataset is what ships)
data/raw/

# Logs & env
*.log
.env
.env.*

# OS / editor noise
.DS_Store
Thumbs.db
